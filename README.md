# 🚀 React TypeScript Vite FSD Template

Современный шаблон для разработки React-приложений с архитектурой Feature-Sliced Design (FSD).

[![TypeScript](https://img.shields.io/badge/TypeScript-5.9.3-blue.svg)](https://www.typescriptlang.org/)
[![React](https://img.shields.io/badge/React-19.2.0-61dafb.svg)](https://react.dev/)
[![Vite](https://img.shields.io/badge/Vite-7.2.4-646cff.svg)](https://vitejs.dev/)
[![FSD](https://img.shields.io/badge/FSD-Architecture-9b59b6.svg)](https://feature-sliced.design/)

## 📋 О проекте

Готовый к использованию шаблон проекта с полной настройкой инструментов разработки, включая:

- ⚡ **Vite** — быстрая сборка и HMR
- ⚛️ **React 19** — последняя версия React
- 📘 **TypeScript** — строгая типизация
- 🎨 **Ant Design** — UI библиотека
- 🔄 **TanStack Query** — управление серверным состоянием
- 🛣️ **React Router** — маршрутизация с декларативными типами
- 🐻 **Zustand** — управление клиентским состоянием
- 📡 **Axios** — HTTP клиент
- 🧪 **Vitest** — тестирование
- 🔍 **ESLint** — линтинг кода
- 💅 **Prettier** — форматирование
- 🐕 **Husky** — Git hooks
- 🎯 **FSD** — архитектура Feature-Sliced Design

## 🏗️ Структура проекта

Проект организован по методологии [Feature-Sliced Design](https://feature-sliced.design/):

```
src/
├── app/              # Инициализация приложения
│   ├── index.tsx     # Точка входа
│   ├── App.tsx       # Корневой компонент
│   ├── providers/    # Провайдеры (Router, QueryClient, Theme)
│   ├── router/       # Конфигурация роутинга
│   └── styles/       # Глобальные стили
│
├── pages/            # Страницы приложения
│   ├── home/         # Главная страница
│   └── about/        # Страница о проекте
│
├── widgets/          # Крупные самостоятельные блоки интерфейса
│   ├── app-layout/   # Основной layout
│   ├── header/       # Шапка сайта
│   └── footer/       # Подвал сайта
│
├── features/         # Функциональность пользователя
│   └── counter/      # Пример фичи (счетчик)
│
├── entities/         # Бизнес-сущности
│   └── user/         # Пример сущности (пользователь)
│
├── shared/           # Переиспользуемый код
│   ├── ui/           # UI компоненты
│   ├── lib/          # Утилиты и хелперы
│   ├── api/          # API клиенты
│   ├── config/       # Конфигурация
│   └── assets/       # Статические ресурсы
│
└── test/             # Настройки тестирования
    └── setup.ts      # Конфигурация тестов
```

## 📚 Слои FSD

### `app/` - Инициализация приложения

- Точка входа приложения
- Провайдеры (Router, QueryClient, Ant Design)
- Глобальные стили
- Настройка роутинга

### `pages/` - Страницы

- Композиция виджетов и фич
- Маршрутизация
- Примеры: `pages/home/`, `pages/about/`

### `widgets/` - Виджеты

- Крупные самостоятельные блоки интерфейса
- Композиция фич и сущностей
- Примеры: `widgets/header/`, `widgets/app-layout/`

### `features/` - Фичи

- Функциональность пользователя
- Композиция сущностей
- Примеры: `features/counter/`, `features/auth/`

### `entities/` - Сущности

- Бизнес-сущности приложения
- Примеры: `entities/user/`, `entities/product/`

### `shared/` - Переиспользуемый код

- `ui/` - UI компоненты (кнопки, инпуты и т.д.)
- `lib/` - Утилиты и хелперы
- `api/` - API клиенты
- `config/` - Конфигурация
- `assets/` - Статические ресурсы

## 🔗 Правила импортов

В FSD действует правило импортов "снизу вверх":

- ✅ `app` может импортировать из всех слоев
- ✅ `pages` может импортировать из `widgets`, `features`, `entities`, `shared`
- ✅ `widgets` может импортировать из `features`, `entities`, `shared`
- ✅ `features` может импортировать из `entities`, `shared`
- ✅ `entities` может импортировать только из `shared`
- ❌ `shared` не может импортировать из других слоев

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:5173](http://localhost:5173)

### Сборка для production

```bash
npm run build
```

### Просмотр production сборки

```bash
npm run preview
```

## 🛠️ Доступные команды

### Разработка

```bash
npm run dev          # Запуск dev сервера
npm run build        # Сборка проекта
npm run preview      # Просмотр production сборки
```

### Качество кода

```bash
npm run lint         # Проверка ESLint и Stylelint
npm run lint:styles  # Проверка только стилей
npm run format       # Форматирование кода с Prettier
npm run format:check # Проверка форматирования
```

### Тестирование

```bash
npm run test         # Запуск тестов в watch режиме
npm run test:ui      # Запуск тестов с UI интерфейсом
npm run test:run    # Однократный запуск тестов
npm run test:coverage # Запуск тестов с отчетом о покрытии
```

## 🧪 Тестирование

Проект настроен для работы с **Vitest** и **React Testing Library**.

Пример теста находится в `src/shared/ui/button/button.test.tsx`

```typescript
import { describe, it, expect, vi } from 'vitest'
import { render, screen } from '@testing-library/react'
import { Button } from './index'

describe('Button', () => {
  it('renders button with text', () => {
    render(<Button>Click me</Button>)
    expect(screen.getByRole('button')).toBeInTheDocument()
  })
})
```

## 🔧 Инструменты разработки

### ESLint

Настроен с правилами для TypeScript, React и обнаружения ошибок. Конфигурация в `eslint.config.js`

### Prettier

Автоматическое форматирование кода. Конфигурация в `.prettierrc`

### Husky

Git hooks для проверки кода перед коммитом. Настроен pre-commit hook для запуска линтера и проверки форматирования.

### TypeScript

Строгая типизация с полной поддержкой типов. Конфигурация в `tsconfig.json`

## 📦 Технологии

| Технология     | Версия  | Описание                        |
| -------------- | ------- | ------------------------------- |
| React          | 19.2.0  | UI библиотека                   |
| TypeScript     | 5.9.3   | Типизированный JavaScript       |
| Vite           | 7.2.4   | Сборщик и dev сервер            |
| Ant Design     | 6.1.1   | UI компоненты                   |
| TanStack Query | 5.90.12 | Управление серверным состоянием |
| React Router   | 7.10.1  | Маршрутизация                   |
| Zustand        | 5.0.9   | Управление состоянием           |
| Axios          | 1.13.2  | HTTP клиент                     |
| Vitest         | 4.0.15  | Тестовый фреймворк              |

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

## 👤 Автор

**maofao**

- GitHub: [@maofao](https://github.com/maofao)

## 🤝 Вклад в проект

Вклад в проект приветствуется! Пожалуйста, создайте issue или pull request.

---

⭐ Если этот проект был полезен, поставьте звезду!
